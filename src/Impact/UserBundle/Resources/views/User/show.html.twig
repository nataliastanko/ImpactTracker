{% extends '::base.html.twig' %}

{% block body -%}
    <div class="page-header">
        <h1>{{entity.fullname}}</h1>

        <ul class="nav nav-pills">
            <li><a href="{{ path('user') }}">Back to the list</a></li>
            <li><a href="{{ path('user_edit', { 'id': entity.id }) }}">Edit</a></li>
            <li class="hide">{{ form(delete_form) }}</li>
        </ul>

    </div>

    <table class="table">
        
            <tr>
                <th>Email</th>
                <td>{{ entity.email }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ entity.lastname }}</td>
            </tr>
            
        </tbody>
    </table>

        <ul class="record_actions">
<<<<<<< Updated upstream
    {#<li>
        <a class="btn" href="{{ path('user_linkedin') }}">
            Connect with LinkedIn
        </a>
    </li>#}
    <li>
        <a href="{{ path('user_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
     <li>
        <a href="{{ path('user') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
=======

    
>>>>>>> Stashed changes
</ul>

<hr>
<h3>LinkedIn connection</h3>
<form action="{{ path('impact_ccc_linkedin_importer_requestPrivate') }}" method="get" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
</form>

<hr>

{% endblock %}
