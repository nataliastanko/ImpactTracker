{% extends '::base.html.twig' %}

{% block body -%}

    <div class="page-header">
        <h1>Survey: {{ entity.title }}</h1>
        <small>{{ entity.description }}</small>
        
        <ul class="nav nav-pills">
            <li><a href="{{ path('survey') }}">Back to the list</a></li>        
            <li><a href="{{ path('survey_edit', { 'id': entity.id }) }}">Edit</a></li>
        </ul>

    </div>

    <div class="page-content">
        <h4>Email content:</h4>
        {{ entity.emailcontent }}
    </div>


        
    {# {{ form(delete_form) }} #}
</ul>
{% endblock %}
