{% extends '::base.html.twig' %}

{% block body -%}

    <div class="page-header">
        <h1>Survey: {{ entity.title }}</h1>
        <a href="{{ path('survey_send', {'id': entity.id }) }}" class="btn btn-primary">Send survey</a>
    </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ entity.title }}</td>
            </tr>
            <tr>
                <th>Email_content</th>
                <td>{{ entity.emailcontent }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
        </tbody>
    </table>

        <a href="{{ path('survey') }}">
            Back to the list
        </a>
        <a href="{{ path('survey_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    
    {{ form(delete_form) }}
</ul>
{% endblock %}
